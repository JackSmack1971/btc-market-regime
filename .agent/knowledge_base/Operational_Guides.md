# OPERATIONAL GUIDES

## 🧠 KNOWLEDGE BASE INTEGRATION (Dispatch Protocol)

Before creating any Implementation Plan, the agent MUST review the following contexts:

1. **Check Global Rules (`GEMINI.md`)**: Ensure compliance with ErrorService and UI styles.
2. **Check Architecture (`Architect.md`)**:
   - IF building a new app: Consult "Stack Selection Logic".
   - IF analyzing crypto data: Consult "Domain Strategy".
3. **Apply Patterns**:
   - If rendering 3D objects, use `THREE.InstancedMesh` (Pattern #7).
   - If refactoring, establish TDD baselines first (Pattern #2).
